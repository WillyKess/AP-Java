public class U6P2Launcher {
    /*
     * use random number to determine if the coin should be flipped at that point,
     * then flip that coin once
     */
    // @param times: the number of times to do this random selection
    public static void flipRandom(Coin[] myCoin, int times) {

    }

    /* traverse the array of coins, flip each coin random times */
    public static void randomFlip(Coin[] myCoin) {

    }

    /*
     * traverse the array of coins, use for each loop to output heads or tails,
     * separate coins by a space and add a new line after every 20 coins
     */
    public static void displayFace(Coin[] myCoin) {

    }

    /*
     * traverse the array of coins, use for each loop to output number of flips from
     * each coin, separate coins by a space and add a new line after every 20 coins
     */
    public static void displayFlips(Coin[] myCoin) {

    }

    /*
     * traverse the array of coins, count how many coins have the current face as
     * heads
     */
    public static int countHeads(Coin[] myCoin) {
        return 0;
    }

    /*
     * traverse the array of coins, if the current face of a coin is not a heads,
     * flip it; DO NOT create new coins
     */
    public static void allHeads(Coin[] myCoin) {

    }
}